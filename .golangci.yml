version: "2"

run:
  timeout: 10m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - unused

formatters:
  enable:
    - goimports
    - golines
  settings:
    golines:
      max-len: 120
